#include <iostream>
#include <string>
#include <fstream>

using namespace std;

class Converter
{
    private:
    
    public:
    Converter();
    static void convertbin(string &fileName1,string &fileName2);
    static void convert(string &fileName1,string &fileName2);
    // fileName on comme nom de fchier fileName.txt a l'interieurs
    
};

// Converter::convert(string...)
// on fait sa si il n'y a pas de construction
